# Google 審査用パッケージ

`GemMurmur-v0.10.2-review.zip` をダウンロードして展開し、Chrome の `chrome://extensions` でデベロッパーモードを有効にしてください。

その後、展開したフォルダ内の `dist` を「パッケージ化されていない拡張機能を読み込む」から選択します。

このパッケージは GemMurmur v0.10.2 のビルド済み配布物です。
